type routerInterface = {
    login: string,
    subscribe: string,
    landing: string,
    landingNacion: string
};

export const ROUTER: routerInterface = {
    login: "/login",
    subscribe: "/subscribe",
    landing: "/",
    landingNacion: "https://www.lanacion.com.ar/"
};